import { StyleSheet } from 'react-native';

export const MeterDataScreenStyle = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  container2: {
    flex: 1,
    alignItems: 'center',
  },
});
